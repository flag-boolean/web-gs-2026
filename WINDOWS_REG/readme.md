# Регистрация протоколов `ssh://` и `rdp://` в Windows через реестр

Инструкция позволяет открывать ссылки вида `ssh://user@host:port` и `rdp://host:port` одним кликом — браузер будет автоматически запускать соответствующие клиенты (PuTTY/OpenSSH или Удалённый рабочий стол).

> ⚠️ **Важно!** Изменение реестра требует прав администратора. Перед применением создайте резервную копию (`Файл → Экспорт` в редакторе реестра) или точку восстановления системы.

---

## 🔧 Требуемые компоненты

| Протокол | Рекомендуемый клиент | Путь по умолчанию |
|----------|----------------------|-------------------|
| `ssh://` | PuTTY (`putty.exe`) | `C:\Program Files\PuTTY\putty.exe` |
| `ssh://` | OpenSSH (встроен в Windows 10/11) | `C:\Windows\System32\OpenSSH\ssh.exe` |
| `rdp://` | Удалённый рабочий стол (`mstsc.exe`) | `C:\Windows\System32\mstsc.exe` |

> 💡 Если используете PuTTY — [скачайте его](https://www.putty.org/) и установите в стандартную папку.


> ✨ После настройки ссылки вида `ssh://admin@srv01` или `rdp://workstation05` станут «живыми» — удобно для документации, панелей управления и внутренних порталов.
